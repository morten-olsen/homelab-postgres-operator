---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: postgresclusters.postgres.homelab.mortenolsen.pro
spec:
  group: postgres.homelab.mortenolsen.pro
  names:
    kind: PostgresCluster
    listKind: PostgresClusterList
    plural: postgresclusters
    singular: postgrescluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PostgresCluster is the Schema for the postgresclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PostgresCluster
            properties:
              image:
                default: pgvector/pgvector:pg18
                description: image is the container image to use for the cluster.
                type: string
            type: object
          status:
            description: status defines the observed state of PostgresCluster
            properties:
              adminConnection:
                description: adminConnection contains the connection details for the
                  admin user.
                properties:
                  host:
                    description: host is the hostname of the postgres cluster.
                    type: string
                  password:
                    description: password is the password for the admin user.
                    type: string
                  port:
                    description: port is the port of the postgres cluster.
                    type: integer
                  url:
                    description: url is the connection url for the admin user.
                    type: string
                  user:
                    description: user is the username for the admin user.
                    type: string
                required:
                - host
                - password
                - port
                - url
                - user
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
