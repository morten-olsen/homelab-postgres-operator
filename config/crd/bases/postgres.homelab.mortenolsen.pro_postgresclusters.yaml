---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: postgresclusters.postgres.homelab.mortenolsen.pro
spec:
  group: postgres.homelab.mortenolsen.pro
  names:
    kind: PostgresCluster
    listKind: PostgresClusterList
    plural: postgresclusters
    singular: postgrescluster
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PostgresCluster is the Schema for the postgresclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PostgresCluster
            properties:
              url:
                description: |-
                  url is the PostgreSQL connection URL.
                  Can be in the format: postgresql://user:password@host:port/database?sslmode=disable
                  Or as a connection string: host=host port=5432 user=user password=password dbname=postgres sslmode=disable
                  Either value or valueFrom must be specified.
                properties:
                  value:
                    description: value is a literal string value.
                    type: string
                  valueFrom:
                    description: valueFrom is a reference to a secret key.
                    properties:
                      key:
                        description: key is the key in the secret to select.
                        type: string
                      name:
                        description: name is the name of the secret in the same namespace
                          as the PostgresCluster.
                        type: string
                      namespace:
                        description: namespace is the namespace of the secret. If
                          not specified, defaults to the PostgresCluster namespace.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state of PostgresCluster
            properties:
              adminConnection:
                description: |-
                  adminConnection contains the connection details for the admin user.
                  Note: Password is not included in status for security reasons.
                properties:
                  host:
                    description: host is the hostname of the postgres cluster.
                    type: string
                  port:
                    description: port is the port of the postgres cluster.
                    type: integer
                  url:
                    description: url is the connection url for the admin user (without
                      password for security).
                    type: string
                  user:
                    description: user is the username for the admin user.
                    type: string
                required:
                - host
                - port
                - url
                - user
                type: object
              conditions:
                description: conditions represent the latest available observations
                  of the PostgresCluster's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  observedGeneration is the most recent generation observed for this PostgresCluster.
                  It corresponds to the PostgresCluster's generation, which is updated on mutation by the API server.
                format: int64
                type: integer
              phase:
                description: phase represents the current phase of the PostgresCluster.
                enum:
                - Pending
                - Creating
                - Ready
                - Failed
                - Deleting
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
